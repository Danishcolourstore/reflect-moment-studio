version: "3.9"

services:
  redis:
    image: redis:7-alpine
    container_name: mirror-ai-redis
    ports:
      - "6379:6379"
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - mirror_ai_redis_data:/data

volumes:
  mirror_ai_redis_data:
